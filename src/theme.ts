import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#0a72ad',
    },
    secondary: {
      main: '#ffc356',
    },
    background: {
      default: '#fcfcfc',
    },
  },
});

export default theme;